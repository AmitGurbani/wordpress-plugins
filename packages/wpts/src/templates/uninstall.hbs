<?php

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}

{{#each settings}}
delete_option( '{{../functionPrefix}}{{key}}' );
{{/each}}
{{#each activationOptions}}
delete_option( '{{this}}' );
{{/each}}
